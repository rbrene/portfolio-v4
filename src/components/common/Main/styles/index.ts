import styled from 'styled-components';
import { animated } from 'react-spring';


export const SCMain = styled(animated.main)`
    position: relative;
    width: 100%;
    height: max-content;
    z-index: inherit;
`;
